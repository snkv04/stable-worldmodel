defaults:
  - _self_
  - override hydra/launcher: submitit_slurm

hydra:
  output_subdir: null
  mode: MULTIRUN
  launcher:
    max_num_timeout: 999
    cpus_per_task: 8
    mem_gb: 256
    timeout_min: 1440
    partition: long-cpu
    signal_delay_s: 120
  run:
    dir: .

cache_dir: null
dataset_name: debug_tworoom

world:
  num_envs: 10
  max_episode_steps: 100
  image_shape: [224, 224]

num_workers: 4
seed: 3072
num_traj: 10000

options: null